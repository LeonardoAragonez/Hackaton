# Pipeline único del monorepo — solo ejecuta backend o frontend según archivos cambiados
name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detectar cambios
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.backend }}
      frontend: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.frontend }}
      shared: ${{ steps.filter.outputs.shared }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'tractor-store-backend/**'
            frontend:
              - 'tractor-store-frontend/**'
            shared:
              - '.github/**'
              - 'sonar-project.properties'
              - 'docker-compose.yml'
              - '.gitignore'
              - 'README.md'
              - 'CONTRIBUTING.md'
              - 'DEPLOYMENT.md'

  backend:
    name: Backend (Java / Maven)
    runs-on: ubuntu-latest
    needs: changes
    if: |
      needs.changes.outputs.backend == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      github.event_name == 'workflow_dispatch'
    defaults:
      run:
        working-directory: tractor-store-backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: tractor-store-backend/pom.xml

      - name: Test & package
        run: mvn -B verify

      - name: Upload JaCoCo report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: tractor-store-backend/target/site/jacoco/

  frontend:
    name: Frontend (Nx / pnpm)
    runs-on: ubuntu-latest
    needs: changes
    if: |
      needs.changes.outputs.frontend == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      github.event_name == 'workflow_dispatch'
    defaults:
      run:
        working-directory: tractor-store-frontend
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: tractor-store-frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: MSW worker (shell)
        run: pnpm exec msw init apps/shell/public --save

      - name: Lint
        run: pnpm lint

      - name: Unit tests with coverage
        run: pnpm test:coverage

      - name: Upload frontend coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: tractor-store-frontend/coverage/

      - name: Production build
        run: pnpm build
        env:
          NODE_ENV: production

  # Envía cobertura (JaCoCo + lcov) al proyecto "Hackaton" en SonarCloud
  sonar-hackaton:
    name: SonarCloud (Hackaton monorepo)
    runs-on: ubuntu-latest
    needs: changes
    if: |
      needs.changes.outputs.backend == 'true' ||
      needs.changes.outputs.frontend == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: tractor-store-backend/pom.xml

      - name: Backend tests & JaCoCo
        working-directory: tractor-store-backend
        run: mvn -B verify

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: tractor-store-frontend/pnpm-lock.yaml

      - name: Frontend tests & lcov
        working-directory: tractor-store-frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm test:coverage

      # lcov se genera con rutas relativas a tractor-store-frontend/ (packages/...).
      # Sonar analiza desde la raíz del repo → hay que prefijar tractor-store-frontend/
      - name: Ajustar rutas lcov para Sonar (monorepo)
        run: |
          find tractor-store-frontend/coverage -name 'lcov.info' -print0 | while IFS= read -r -d '' f; do
            sed -i -E \
              -e 's|^SF:packages/|SF:tractor-store-frontend/packages/|g' \
              -e 's|^SF:apps/|SF:tractor-store-frontend/apps/|g' \
              "$f"
          done

      - name: Verificar reportes de cobertura
        run: |
          test -f tractor-store-backend/target/site/jacoco/jacoco.xml
          find tractor-store-frontend/coverage -name 'lcov.info' -print | tee /dev/stderr | grep -q .
          grep -m1 '^SF:tractor-store-frontend/' tractor-store-frontend/coverage/packages/shared-catalog/lcov.info

      - name: SonarCloud scan (con cobertura en esta rama)
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >
            -Dsonar.branch.name=${{ github.head_ref || github.ref_name }}
            -Dsonar.java.coveragePlugin=jacoco
            -Dsonar.coverage.jacoco.xmlReportPaths=tractor-store-backend/target/site/jacoco/jacoco.xml
            -Dsonar.typescript.lcov.reportPaths=tractor-store-frontend/coverage/packages/shared-catalog/lcov.info,tractor-store-frontend/coverage/packages/ts-design-system/lcov.info

  sonar-backend:
    name: SonarCloud (backend)
    runs-on: ubuntu-latest
    needs: [changes, backend]
    if: |
      always() &&
      needs.backend.result == 'success' &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    defaults:
      run:
        working-directory: tractor-store-backend
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: tractor-store-backend/pom.xml

      - run: mvn -B verify

      # secrets no se pueden usar en job `if`; sin SONAR_TOKEN este paso falla pero no rompe el pipeline
      - uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true

  sonar-frontend:
    name: SonarCloud (frontend)
    runs-on: ubuntu-latest
    needs: [changes, frontend]
    if: |
      always() &&
      needs.frontend.result == 'success' &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    defaults:
      run:
        working-directory: tractor-store-frontend
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: tractor-store-frontend/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm test:coverage || pnpm test

      - uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true

  docker-backend:
    name: Docker image (backend)
    runs-on: ubuntu-latest
    needs: [changes, backend]
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.backend.result == 'success'
    defaults:
      run:
        working-directory: tractor-store-backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: tractor-store-backend/pom.xml

      - run: mvn -B -DskipTests package

      - name: Build image
        run: docker build -t tractor-store-backend:${{ github.sha }} .

  docker-frontend:
    name: Docker image (frontend)
    runs-on: ubuntu-latest
    needs: [changes, frontend]
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.frontend.result == 'success'
    defaults:
      run:
        working-directory: tractor-store-frontend
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: tractor-store-frontend/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm exec msw init apps/shell/public --save
      - run: pnpm build
        env:
          NODE_ENV: production

      - name: Build image
        run: docker build -t tractor-store-frontend:${{ github.sha }} .
